#include <iostream>
#include <string>

using namespace std;

int main() {
    cin.tie(0);
    ios::sync_with_stdio(0);

    string s{}, t{};
    cin >> s;
    cin >> t;

    cout << t << "\n" << s << "\n";
    return 0;
}